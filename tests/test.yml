---

- hosts: molecule-ubuntu
  become: true
  become_method: sudo
  vars_files:
    - environments/vagrant-vm.yml
  roles:
    - base
    - ntp
    - java
    - maven
    - ansible
    - docker
    - monit
    - mmonit

- hosts: molecule-centos
  become: true
  become_method: sudo
  vars_files:
    - environments/vagrant-vm.yml
  roles:
    - base
    - ntp
    - java
    - maven
    - ansible
    - docker
    - mmonit
    - monit
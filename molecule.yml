---

ansible:
  config_file: ~/Workspace/ck/cheetah/jumpstart/ansible.cfg

dependency:
  name: galaxy

driver:
  name: docker

docker:
  containers:
    - name: molecule-ubuntu
      image: ubuntu
      image_version: latest
      ansible_groups:
        - molecule

verifier:
  name: testinfra